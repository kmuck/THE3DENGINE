export module engine.platform:input;

import std;

namespace engine::platform {

    // export bool is_key_pressed(KeyCode key);
    // export bool is_key_released(KeyCode key);
    // export std::pair<int, int> mouse_position();

} // namespace engine::platform
